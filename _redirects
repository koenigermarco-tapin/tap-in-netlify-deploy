# Netlify redirects file
# This file MUST be in the publish directory root

# Ensure all HTML files are served correctly
/*.html 200

# Navigator is the main entry point
/navigator /stripe-navigator.html 200

# Force no caching on HTML
/*.html
  Cache-Control: no-cache, no-store, must-revalidate
